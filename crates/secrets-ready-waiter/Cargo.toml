[package]
name = "secrets-ready-waiter"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
nix = { version = "0.29", features = ["user"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
